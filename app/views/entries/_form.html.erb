<%= form_with(model: entry, local: true) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :entry_title , class:"form_control" %>
  </div>

  <div class="field">
    <%= form.label :downtitle %>
    <%= form.text_area :downtitle, id: :entry_downtitle , class:"form_control" %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :entry_body , class:"form_control" %>
  </div>

  <div class="actions">
    <%= form.submit "Ingresar", class:"btn be-green white" %>
  </div>
<% end %>
